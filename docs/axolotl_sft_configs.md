После этапа CPT ваша модель похожа на энциклопедию: она в совершенстве знает русский язык, но не понимает, что с ней разговаривают. Если вы спросите её: *"Как сварить борщ?"*, она, скорее всего, продолжит мысль: *"Как сварить пельмени? Как испечь пирог?"*.
Чтобы превратить её в умного ассистента, нужен этап **SFT (Supervised Fine-Tuning)**. На этом шаге мы учим модель формату диалога и правилам хорошего тона.
Процесс SFT концептуально отличается от CPT. Вот три главных шага для правильной настройки.
**Внедрение Chat Template (Шаблона диалога)**
*Разделение ролей*
Модель должна четко понимать, где заканчивается вопрос человека и начинается её собственный ответ. Для этого используются спецсимволы (токены). Самый популярный стандарт сегодня — ChatML. Ваш текст будет автоматически оборачиваться так:
&lt;|im_start|&gt;user\nКак дела?&lt;|im_end|&gt;\n&lt;|im_start|&gt;assistant\nВсё отлично!&lt;|im_end|&gt;**Маскирование Loss (train_on_inputs: false)**
*Самое важное правило SFT*
При CPT модель училась предсказывать каждое следующее слово. В SFT так делать нельзя! Если модель будет учиться на вопросах пользователя, она начнет генерировать вопросы вместо ответов. Мы должны вычислять ошибку (Loss) и обновлять веса только на тех словах, которые произносит assistant. Axolotl делает это автоматически с помощью одной настройки.**Подготовка диалоговых данных**
*ShareGPT формат*
Сырые тексты больше не подходят. Вам нужен датасет качественных инструкций в формате диалогов. Золотой стандарт для русского языка — датасеты от Ильи Гусева (например, IlyaGusev/ru_instruct или saiga_scialign).
## Как выглядят данные для SFT
Axolotl лучше всего работает с форматом **ShareGPT**. Ваши данные должны лежать в JSONL-файле, где каждая строка — это отдельный диалог.
Пример одной строки файла sft_data_ru.jsonl:
```json
{
  "conversations": [
    {"from": "human", "value": "Напиши короткое стихотворение про осень."},
    {"from": "gpt", "value": "Осень в город незаметно\nЗакатила желтый шар.\nВетер сдул листву с деревьев,\nБросив на асфальт пожар."}
  ]
}

```
## Конфиг Axolotl для SFT
Вы можете использовать тот же фреймворк Axolotl и тот же сервер, но конфиг sft_lfm_ru.yml будет существенно отличаться от CPT-версии:
```yaml
# Базовая модель — это результат вашего CPT
base_model: ./out_cpt/checkpoint-final
model_type: AutoModelForCausalLM
tokenizer_type: AutoTokenizer
trust_remote_code: true

# 1. Настройка диалогового формата
chat_template: chatml        # Указываем формат спецтокенов
train_on_inputs: false       # КРИТИЧЕСКИ ВАЖНО: не учимся на вопросах пользователя!

# 2. Датасеты
datasets:
  - path: json
    data_files: ./data/sft_data_ru.jsonl
    type: sharegpt           # Тип данных изменен с pretrain на sharegpt

# 3. Гиперпараметры SFT
# Для SFT нужен Learning Rate еще ниже, чем для CPT, чтобы не забыть знания
learning_rate: 1e-5          
num_epochs: 2                # В SFT можно пройтись по данным 2-3 раза
micro_batch_size: 2
gradient_accumulation_steps: 8
sequence_len: 4096           # Максимальная длина диалога
sample_packing: true

# 4. Сохранение и архитектура
bf16: auto
gradient_checkpointing: true
deepspeed: deepspeed_configs/zero2.json
output_dir: ./out_sft

```
## Процесс запуска
Команды остаются точно такими же, как и при CPT. Сначала вы кэшируете диалоги:
```bash
CUDA_VISIBLE_DEVICES="" python -m axolotl.cli.preprocess sft_lfm_ru.yml

```
Затем запускаете распределенное обучение:
```bash
accelerate launch -m axolotl.cli.train sft_lfm_ru.yml

```
> **Индикатор успеха:** Во время SFT внимательно следите за графиком **Loss** (функцией потерь). В отличие от CPT, где loss падает медленно, в SFT он часто стремительно летит вниз в первый же час обучения. Если loss опускается ниже 0.5, модель может начать зазубривать ответы наизусть (overfitting). Лучшие SFT-модели обычно останавливают обучение на показателе loss в районе 0.8 - 0.9.
>
